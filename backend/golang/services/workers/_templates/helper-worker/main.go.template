package main

import (
	"context"
	"github.com/aws/aws-lambda-go/events"
	"github.com/aws/aws-lambda-go/lambda"
	"myfusionhelper.ai/internal/helpers"
	"myfusionhelper.ai/internal/helpers/{{CATEGORY}}"
)

func main() {
	// Register ONLY {{HELPER_TYPE_SNAKE}} helper
	helpers.Register("{{HELPER_TYPE_SNAKE}}", {{CATEGORY}}.New{{HELPER_NAME_PASCAL}})
	lambda.Start(Handle)
}

func Handle(ctx context.Context, event events.SQSEvent) error {
	// Worker logic here
	return nil
}
